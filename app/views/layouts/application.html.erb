<!DOCTYPE html>
<html>
  <head>
    <title>VanillaRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav>
      <% if !user_signed_in? %>
        <div><%= link_to "Sign up", new_user_registration_path %></div>
        <div><%= link_to "Sign in", new_user_session_path %></div>
      <% else %>
        <div><%= link_to "Edit profile", edit_user_registration_path, method: :patch%></div>
        <div><%= link_to "Sign out", destroy_user_session_path, method: :delete %></div>
      <% end %>
    </nav>
    <%# <p style="color: green;"><%= notice %></p> 
    <%# <p style="color: red;"><%= alert %></p> 
    <%= yield %>
  </body>
</html>
